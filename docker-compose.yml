

services:
  web:
    build: .
    container_name: api_decrypt_web
    command: >
      sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8080"
    volumes:
      - .:/api-decrypt
    ports:
      - "127.0.0.1:8080:8080"
